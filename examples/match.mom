// match.mom — exhaustive pattern matching on integers.

fn classify(code: Int) -> String {
    match code {
        200 => "ok",
        404 => "missing",
        _   => "error",
    }
}

fn main() {
    print(classify(200))
    print(classify(500))
}
