// fib.mom — stage-1.2 demo: recursion, if/else, function calls.
// Expected output: 55

fn fib(n: Int) -> Int:
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

fn main():
    print(fib(10))
