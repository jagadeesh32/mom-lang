// fibonacci.mom — stage-1 demo: recursive fibonacci.
// Expected output: 55
//
// Requires: stage-1 compiler with str() + println support.

fn fib(n: Int) -> Int:
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

fn main():
    println(str(fib(10)))
