// fibonacci.mom — stage-1.2 demo: recursive fibonacci with println(str()).
// Expected output: 55

fn fib(n: Int) -> Int:
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)

fn main():
    println(str(fib(10)))
