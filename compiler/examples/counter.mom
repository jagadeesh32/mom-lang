// counter.mom — stage-1.2 demo: while loop + str(int) + println.
// Expected output: 0 1 2 3 4 (one per line)

fn main():
    let mut i = 0
    while i < 5:
        println(str(i))
        i = i + 1
