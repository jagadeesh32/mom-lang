// factorial.mom — stage-1.2 demo: while loop + mutable state.
// Expected output: 5040

fn factorial(n: Int) -> Int:
    let mut result = 1
    let mut i = 1
    while i <= n:
        result = result * i
        i = i + 1
    return result

fn main():
    print(factorial(7))
